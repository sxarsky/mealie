// Generated by Skyramp v1.3.29 on 2026-06-25 07:36:10.847465244 +0000 UTC m=+618.119707241
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output-dir /home/runner/work/mealie/mealie/tests/skyramp/frontend \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/mealie/mealie/.skyramp/group_data_bulk_delete_count_trace.zip

// Import of required libraries
import { test, expect, getValue, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(876000);

    const errors: Error[] = [];
    page.on('pageerror', err => errors.push(err));

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto("http://localhost:9000/login");
    await page.locator("#username").fill("admin@mealie.test");
    await page.locator("#password").fill("MyPassword");
    await page.getByRole("button", { name: "Login" }).click();
    // Wait for network activities to finish
    await page.waitForTimeout(3000);

    const ts = Date.now();
    await page.getByText("Organizers").click();
    await page.getByRole("link", { name: "Categories" }).click();
    await page.getByRole("main").getByRole("button", { name: "Create" }).click();

    const playwrightRequest0: Record<string, any> = {"name": `Test Category Alpha ${ts}`};

    await page.getByRole("textbox", { name: "Category Name" }).fill(getValue(playwrightRequest0, "name"));
    // Wait to finish any potential hydration
    await page.waitForTimeout(1500);
    const responsePromise0 = page.waitForResponse("**/api/organizers/categories**");
    await page.getByRole("dialog").getByRole("button", { name: "Create" }).click();
    const response0 = await responsePromise0;
    expect(response0.status()).toBe(201);

    // Wait for network activities to finish
    await page.waitForTimeout(3000);

    await page.getByRole("main").getByRole("button", { name: "Create" }).click();

    const playwrightRequest1: Record<string, any> = {"name": `Test Category Beta ${ts}`};

    await page.getByRole("textbox", { name: "Category Name" }).fill(getValue(playwrightRequest1, "name"));
    // Wait to finish any potential hydration
    await page.waitForTimeout(1500);
    const responsePromise1 = page.waitForResponse("**/api/organizers/categories**");
    await page.getByRole("dialog").getByRole("button", { name: "Create" }).click();
    const response1 = await responsePromise1;
    expect(response1.status()).toBe(201);

    // Wait for network activities to finish
    await page.waitForTimeout(3000);

    await page.getByRole("link", { name: getValue(playwrightRequest0, "name") }).hover();
    await page.getByRole("link", { name: getValue(playwrightRequest0, "name") }).getByRole("button").click();
    await page.keyboard.press("Escape");
    await page.goto("http://localhost:9000/group/data/categories");
    await page.locator("main").getByRole("checkbox", { name: "Select all" }).click();
    await page.getByRole("button", { name: "Actions" }).click();
    await page.locator(".v-list-item.v-list-item--link.v-theme--light.v-list-item--density-compact > .v-list-item__prepend").click();
    await expect(page.locator('[data-total-count]')).toHaveAttribute('data-total-count', '2');
    await expect(page.getByRole("button", { name: "Confirm" })).toBeVisible();
    await expect(page.getByText("Are you sure you want to")).toContainText("Are you sure you want to delete the following items?");
    await page.getByRole("button", { name: "Cancel" }).click();
    expect(errors).toHaveLength(0);
});
