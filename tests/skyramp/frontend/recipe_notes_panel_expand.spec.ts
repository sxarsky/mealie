// Generated by Skyramp v1.3.37 on 2026-08-09 04:02:14.279012936 +0000 UTC m=+499.869146394
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output recipe_notes_panel_expand.spec.ts \
// 		--output-dir /home/runner/work/mealie/mealie/tests/skyramp/frontend \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/mealie/mealie/.skyramp/recipe_notes_panel_expand_trace.zip

// Import of required libraries
import { test, expect, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(120000);

    const errors: Error[] = [];
    page.on('pageerror', (err) => errors.push(err));

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto("http://localhost:9000/login");
    await page.getByRole("textbox", { name: "Email or Username" }).fill("admin@mealie.test");
    await page.getByRole("textbox", { name: "Password" }).fill("MyPassword");
    await page.getByRole("button", { name: "Login" }).click();
    await page.waitForURL("**/g/home");

    await page.goto("http://localhost:9000/g/home/r/test-recipe-with-notes");
    await page.waitForLoadState('networkidle');
    await page.getByRole("button", { name: "Note" }).waitFor({ state: 'visible', timeout: 10000 });

    await page.getByRole("button", { name: "Note" }).click();

    await expect(page.getByRole("heading", { name: "Note" })).toBeVisible();
    await expect(page.getByRole("button", { name: "Note" })).toHaveAttribute("aria-expanded", "true");

    expect(errors).toHaveLength(0);
});
