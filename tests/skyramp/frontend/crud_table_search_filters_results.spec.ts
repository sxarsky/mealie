// Generated by Skyramp v1.3.29 on 2026-06-25 07:33:08.598673846 +0000 UTC m=+799.592297788
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output crud_table_search_filters_results.spec.ts \
// 		--output-dir /home/runner/work/mealie/mealie/tests/skyramp/frontend \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/mealie/mealie/.skyramp/crud_table_search_filters_results_trace.zip

// Import of required libraries
import { test, expect, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(255000);

    const errors: Error[] = [];
    page.on('pageerror', (err) => errors.push(err));

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto("http://localhost:9000/group/data/foods");
    await page.waitForLoadState('networkidle');
    await page.goto("http://localhost:9000/login");
    await page.waitForLoadState('networkidle');
    await page.locator("#username").fill("admin@mealie.test");
    await page.locator("#password").fill("MyPassword");
    await page.getByRole("button", { name: "Login" }).click();
    await page.goto("http://localhost:9000/group/data/foods");
    await page.waitForLoadState('networkidle');
    await page.locator('text=1-15 of').waitFor({ state: 'visible', timeout: 30000 });
    await page.getByRole("textbox", { name: "Search" }).fill("salt");
    await expect(page.getByText("-15 of 19")).toContainText("1-15 of 19");
    await expect(page.getByRole("textbox", { name: "Search" })).toHaveValue("salt");
    await expect(page.getByRole("cell", { name: "curing salt" }).first()).toHaveText("curing salt");
    await expect(page.getByTestId("crud-search-no-match")).not.toBeVisible();
    expect(errors).toHaveLength(0);
});
